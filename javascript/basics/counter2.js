// What will the following code snippets log?

function counter(count) {
  // ...
}

console.log('The total value is ' + (counter * rate));

var counter = 5;
var rate = 3;

// Logs: "The total values is NaN"
